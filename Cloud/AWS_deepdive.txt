PROBLEMS WITH TRADITIONAL ON-PREMISES APPROACH
- High Capital & Operational Costs: Significant upfront investment in hardware plus continuous spending on rent, power, and cooling, regardless of usage.
- Slow Provisioning & Upgrades: Adding new capacity is a manual process involving procurement and physical setup, delaying critical business projects.
- Heavy Operational Burden: Requires a dedicated 24/7 team for monitoring and maintenance, diverting skilled staff from innovation to basic upkeep.
- Vulnerability to Disasters: A single data center is a single point of failure, making the business vulnerable to fires, floods, or power outages.
- Limited & Inefficient Scaling: Capacity is fixed. You must either over-provision (and waste money) for peak loads or risk failure during traffic spikes.

WHAT IS CLOUD COMPUTING?
Cloud computing is the delivery of computing services—including servers, storage, databases, networking, and software—over the internet. Instead of buying or keeping hard drives and physical servers, you rent access to these tools from a provider like Amazon Web Services and pay only for what you use.

CLOUD DEPLOYMENT MODELS
- Private Cloud: A dedicated cloud environment used exclusively by a single organization. Provides maximum control and privacy. Requires organization to manage costs and maintenance.
- Public Cloud: Resources owned/operated by a provider (like AWS) and delivered over the internet. Massive scalability and pay-as-you-go model.
- Hybrid Cloud: A strategic blend of private and public cloud resources. Keeps critical systems on-premises while using the cloud for other workloads.

ADVANTAGES OF CLOUD COMPUTING
- Flexibility & Adaptability: Easily change resource types, OS, or software on the fly.
- Cost-Effectiveness: Trade CapEx for OpEx. Pay only for consumed resources.
- Scalability for Growth: Handle increased workloads by scaling up (power) or scaling out (more resources).
- Elasticity for Demand: Automatically scale resources up or down in response to real-time demand.
- High Availability & Fault Tolerance: Built-in resilience by deploying applications across multiple data centers.
- Agility & Speed: Reduce provisioning time from weeks to minutes.

CLOUD MARKET SHARE
- AWS: 39.2%
- Azure: 25.3%
- GCP (Google Cloud): 16.5%
- Alibaba: 11.4%
- IBM: 5.1%
- Oracle: 2.5%

TYPES OF CLOUD COMPUTING
- Infrastructure as a Service (IaaS): Fundamental building blocks. Access to virtual servers, storage, networking. Highest flexibility. (Analogy: Leasing land and building materials).
- Platform as a Service (PaaS): Provider manages infrastructure (hardware/OS). You focus on deploying/managing applications. Ideal for developers. (Analogy: Leasing land and a pre-built foundation).
- Software as a Service (SaaS): Complete, ready-to-use software product delivered over the internet. Provider manages everything. (Analogy: Renting a fully furnished house). example AWS QuickSight

AWS GLOBAL INFRASTRUCTURE
- Regions: Distinct geographical areas around the world where AWS clusters its data centers.
- Availability Zones (AZs): Multiple isolated locations within each Region. Distributing resources across AZs builds highly available/fault-tolerant applications.
- Local Zones: Extension of an AWS Region bringing core services closer to large population centers for latency-sensitive applications.
- Edge Locations: Global network of sites that cache content close to users for faster delivery.
- AWS Regional Edge Caches: Positioned between origin servers and Edge Locations. Hold larger amounts of content to reduce fetching from origin. eg: cloud front
- Wavelength Zones: Embed AWS compute/storage directly within 5G networks for ultra-low latency.

EC2 - ELASTIC COMPUTE CLOUD
Amazon EC2 provides secure, resizable virtual servers (instances) in the cloud.

AMAZON EC2 INSTANCE TYPES
- General purpose (M and T family): Balance of compute, memory, and networking. Ideal for web servers.
- Compute optimized (C family): For CPU-bound workloads requiring significant processing power (e.g., scientific modeling, video encoding, gaming servers).
- Memory optimized (R, U, X, Z family): For processing large datasets in memory (e.g., SAP HANA, Spark, Redis). Lowest cost per GiB of RAM.
- Storage optimized (D, H, I family): For high, sequential read/write access to large datasets on local NVMe SSDs (e.g., NoSQL databases like Cassandra, Data warehousing).
- Accelerated computing (P, G, Inf family): Hardware accelerators (GPUs, AWS Trainium) for machine learning, 3D visualizations, graphics processing.
- High-performance computing (HPC family): Best price-performance for complex HPC workloads (e.g., weather forecasting, genomic sequencing) using high-speed networking (EFA).

AMI - AMAZON MACHINE IMAGE
A master template or blueprint for a server (pre-configured package).
- Includes: Operating System (Linux, Windows), additional software (web server, DB), and necessary configurations.
- "Golden Image": Create the setup once on a single instance, save it perfectly configured as a custom AMI, and then launch identical servers instantly in minutes.

A Placement Group is an AWS feature that lets you influence the physical placement of your Amazon EC2 instances within the underlying hardware of an AWS Data Center.

Types:
  Cluster Placement Group -> Packs instances close together inside a single Availability Zone.
  Spread Placement Group -> Places a small group of instances on completely distinct underlying hardware racks, each with independent power and network sources.
  Partition Placement Group -> Spreads your instances across logical chunks called partitions.
  Precision Time Placement Group -> Places instances onto specialized hardware that features direct, low-latency access to high-precision clock sources (such as NTP time services).

Instance profile -> An AWS instance profile is a container that holds an IAM role and passes its permissions to an Amazon EC2 instance at startup.

AMAZON EC2 PRICING MODELS
- On-Demand: Pay-per-second/hour. No upfront costs. Best for unpredictable workloads or dev/test environments.
- Reserved Instances: Up to 75% savings for 1-3 year commitments. Best for steady-state production. (Standard and Convertible options).
- Savings Plans: Up to 72% savings over 1-3 years for flexible compute commitments.
- Spot Instances: Up to 90% savings using spare AWS capacity. Can be interrupted with a 2-minute warning. Best for fault-tolerant, batch processing workloads.
- Dedicated Hosts: Rent entire physical servers. Required for BYOL (Bring Your Own License) and strict compliance.
- Capacity Reservations: Guarantees compute capacity in specific AZs. Pay On-Demand rates only when used. Critical for disaster recovery and peak events.

example: Amazon EC2 Pricing using a standard general-purpose m5.large instance (Linux, US East/N. Virginia region) as a concrete baseline example: per hour
1. ondemand: 0.096 USD
2. reserved instance: 0.060 USD (1-year) 0.041 USD (3-years)
3. savings plan: 0.060 USD (1-year) 0.041 USD (3-years)
4. spot instance: btwn 0.043-0.047 USD
5. dediated host: 5.069 USD

2/2 or 3/3 status checks done while connecting to a ec2 instance
    1. System Status Check --> it checks wether the AWS host hardware OK? such as power, network connectivity, and the hypervisor.
    2. Instance Status Check --> It checks is your operating system reachable? 
    3. Attached EBS Status Check --> It checks Can the attached EBS volumes perform I/O properly?

AWS IAM - Identity and Access Management -->AUTHENTICATION (AuthN) VS AUTHORIZATION (AuthZ)
- Authentication: Verifying identity ("Who are you?"). e.g., Username/Password, Access Keys, MFA.
- Authorization: Granting permissions ("What are you allowed to do?"). e.g., IAM Policies, Roles. 
- Rule: AuthN always precedes AuthZ. You can be authenticated but have zero authorization.

IAM CORE COMPONENTS
- IAM User: Represents a person or application with long-term credentials (Password for Console, Access/Secret Keys for CLI/API).
- IAM Group: Collection of IAM users. Used to manage permissions at scale.
  * Rules: Users can be in up to 10 groups. No nested groups. Cannot assign roles to groups.
- IAM Role: Temporary access credentials assumable by trusted entities (users, services, federated identities). Powered by AWS STS (Security Token Service).
- IAM Policy: JSON documents defining permissions. Attached to identities or resources.

The Principle of Least Privilege (PoLP) -> It means giving users, programs, and systems only the bare minimum access and permissions they need to do their specific jobs, and nothing more

AWS STS secure token service -> a web service that lets you request temporary, limited-privilege credentials for IAM users or federated users.

DEEP DIVE: IAM ROLES (HIGH YIELD FOR INTERVIEWS)
Unlike users, roles do not have long-term credentials. They provide temporary security credentials (valid 15 mins to 12 hours) via STS.
A role requires two policies:
1.Trust Policy (Resource-based policy): This defines WHO is allowed to assume the role. It is literally a policy attached to the role that answers the question: "Who do I trust to wear this identity?" This could be an AWS service (like EC2), a specific IAM user, or another AWS account entirely.
2.Permission Policy (Identity-based policy): This defines WHAT the principal can do once they have successfully assumed the role. It answers the question: "What AWS actions can this role perform, and on which resources?" (e.g., Read access to an S3 bucket or full access to DynamoDB).

DEEP DIVE: IAM POLICIES & EVALUATION LOGIC
Policy JSON Structure Elements: 
- Effect (Allow/Deny), Action (e.g., s3:GetObject), Resource (ARN), Condition (Optional, e.g., specific IP or MFA).

Types of Policies:
- Identity-Based: Attached to Users, Groups, or Roles.
  * AWS Managed: Built-in, maintained by AWS (e.g., AdministratorAccess).
  * Customer Managed: Custom, version-controlled by you. (Best practice).
  * Inline: 1:1 relationship embedded directly in an entity (Use sparingly).
- Resource-Based: Attached directly to a resource (e.g., S3 Bucket Policy). Essential for cross-account access. Must contain a "Principal" element.

Policy Evaluation Logic (CRITICAL)
1. Explicit Deny: Always overrides any Allow.
2. Explicit Allow: Grants access if no Explicit Deny exists.
3. Default Deny (Implicit Deny): If there is no Allow and no Deny, access is denied.

What are the roles in AWS does developer need to get for temporary access
To get temporary access in AWS, a developer typically needs an IAM role that assumes specific permissions via AWS STS (Security Token Service). Common roles include read-only or power-user roles for development environments, managed via AWS IAM Identity Center (SSO) with short-term credentials lasting from one to 12 hours.

AWS SYSTEMS MANAGER (SSM)- default port is 443
WHAT IS AWS SYSTEMS MANAGER (SSM)?
A centralized management service for EC2 instances and on-premises servers. It allows you to automate operational tasks across your AWS resources without needing SSH, key pairs, or open inbound ports.

HOW SSM WORKS (THE AGENT MODEL)
- SSM uses an Agent-based model. The SSM Agent runs ON your instance.
- The Agent makes OUTBOUND calls to the SSM service over HTTPS (port 443). 
- The SSM service NEVER calls IN to your instance, meaning you DO NOT need to open any inbound ports (like Port 22 for SSH).

THREE PREREQUISITES FOR SSM TO WORK (CRITICAL)
1. SSM Agent Installed: Pre-installed on Amazon Linux and Ubuntu. Must be manually installed on some others (RHEL, CentOS).
2. IAM Role: The instance must have an IAM role attached with the 'AmazonSSMManagedInstanceCore' policy.
3. Network Connectivity: The instance must be able to reach SSM endpoints (via Internet Gateway, NAT Gateway, or VPC Endpoints).

CORE CAPABILITIES (HIGH YIELD FOR INTERVIEWS)
A. SESSION MANAGER (The "SSH Killer")
- Purpose: Secure, browser-based or CLI terminal access to instances.
- Benefits over SSH: No port 22 needed, no key pairs, no bastion hosts required.
- Security/Auditing: Every command typed is logged to S3 or CloudWatch and tied to the IAM user identity (great for compliance).
B. RUN COMMAND
- Purpose: Execute scripts/commands on multiple instances simultaneously without connecting to them.
- Targeting: Best practice is to target instances using Tags (e.g., Environment=Production).
- Rate Control (Crucial for safety):
  * Concurrency: How many instances run the command at the same time.
  * Error Threshold: How many instances can fail before the entire operation is halted.
C. PARAMETER STORE vs SECRETS MANAGER
- Parameter Store: Key-value store for configuration (String, StringList) and secrets (SecureString - encrypted via KMS). Standard tier is FREE. Does not natively support auto-rotation.
- Secrets Manager: Costs money ($0.40/secret/month). Supports native, automatic rotation of database passwords (e.g., RDS) and cross-account access.
- Rule of Thumb: Use Parameter Store for general config and static secrets; use Secrets Manager when auto-rotation is strictly required.
D. PATCH MANAGER
- Purpose: Automates OS patching across your fleet.
- Key terms: 
  * Patch Baseline (rules defining which patches to auto-approve).
  * Maintenance Window (scheduled time to apply patches to avoid business disruption).
E. STATE MANAGER
- Purpose: Configuration management. Ensures instances maintain a "desired state" (e.g., ensuring a specific agent is always running) to prevent configuration drift.
F. INVENTORY
- Purpose: Automatically collects metadata from instances (installed software, OS versions, running services). Useful for security audits (e.g., finding out which servers have a vulnerable software version).

Amazon EBS (Elastic Block Store)
Amazon Elastic Block Store (EBS) is a high-performance, block-level storage service designed for use with Amazon EC2 virtual servers in the AWS cloud. 

What Problem Does EBS Solve?
•	Data Persistence: "The most critical problem EBS solves is data persistence. With local instance storage, if an EC2 instance crashes or is terminated, all that data is gone forever. EBS decouples the storage from the compute layer, meaning your data survives independently and can easily be reattached to a new instance if the original one fails."
•	Seamless Capacity Scaling: "It also solves the headache of running out of space. Without EBS, hitting your storage limit meant resizing the entire machine or migrating workloads. With EBS, you can dynamically increase your volume size or attach a brand new volume on the fly, with zero downtime."
•	Dynamic Performance Tuning: "Similarly, EBS allows for real-time performance adjustments. If an application suddenly requires faster read/write speeds, you can upgrade the volume type—for example, moving from a standard gp3 volume to a high-performance io2 volume—without having to migrate data to a new server."
•	Simplified Backups: "EBS drastically simplifies disaster recovery. Instead of engineering complex, manual backup scripts to save local files, EBS allows you to take one-click, incremental snapshots that are automatically and securely stored in Amazon S3."
•	Data Mobility: "It also makes moving data much faster. Rather than copying gigabytes of files across a network to transfer data between servers, you can simply detach an EBS volume from one instance and immediately mount it to another."
•	Effortless Security: "Finally, EBS streamlines security and compliance. Rather than configuring complex, OS-level encryption yourself, EBS provides a native, one-click solution that uses AES-256 to encrypt your data both at rest and in transit."

What is snapshot
    A snapshot is a complete, point-in-time capture of a system's exact state, whether that means saving an entire file tree in Git or backing up a storage drive in cloud infrastructure. Across all of these tools, its primary purpose is to provide a reliable, historical record that you can instantly revert to if something goes wrong.

EBS Volume Types:
SSD-Based (Random I/O — databases, boot volumes):
  ├── gp3 / gp2  — General Purpose SSD  (the Honda Civic — reliable, affordable)
  └── io2 / io1  — Provisioned IOPS SSD  (the Ferrari — expensive, blazing fast)
HDD-Based (Sequential I/O — big data, logs, archives):
  ├── st1  — Throughput Optimized HDD  (the cargo truck — moves lots of data)
  └── sc1  — Cold HDD  (the warehouse — cheap, rarely accessed)

AWS VPC (Virtual Private Cloud) is a  Virtual Private Cloud (VPC) in cloud computing is a logically isolated, private network within a public cloud

difference between ipv4 and ipv6
 IPv4 uses a 32-bit architecture (yielding ~4.3 billion addresses), while IPv6 uses a 128-bit architecture (yielding 340 undecillion addresses) to solve the global problem of running out of unique IPv4 addresses.

address allocation
Address allocation in Amazon Web Services (AWS) manages how public and private IP addresses are assigned to your cloud resources.
VPC IP Address Manager (IPAM): A tool that helps you plan, track, and monitor IP addresses for your workloads.

CIDR
Classless Inter-Domain Routing, is a method for allocating IP addresses and routing internet traffic more efficiently.

For example: 10.0.0.0/16
            └── This means: the first 16 bits are FIXED, the rest can change
To get avalible ip's we need to use (2 power 32-16) =2 power 16 =65,536 Available IPs
Out of these 5 are reserved for 
  1.Network address (can never be used)
  2.AWS reserves for the VPC router
  3.AWS reserves for DNS
  4.AWS reserves for future use
  5.Broadcast address (not used in VPC but reserved anyway)

The largest single CIDR block you can assign is /16 (65,536 IP addresses), and the smallest single CIDR block you can assign is /28 (16 IP addresses).

VPC COMPONENTS
1. Internet Gateway -> connects your Virtual Private Cloud (VPC) to the public internet.
2. Route tables
3. NACL(Network access control list)-> inbound traffic and outbound traffic at subnets level and has ability to allow and denay rules
4. public/private subnets
5. Security groups-> inbound traffic at instance level and has ability to only allow rules
6. NAT gateways -> a managed service that lets resources in a private subnet connect to the internet or other external services.

bastion host / jump server -> It is a specialized Amazon EC2 instance placed in a public subnet. It acts as a secure, controlled gateway allowing administrators to connect via SSH or RDP to other EC2 instances residing in private subnets that have no direct internet access.

vpc peering
it is a direct networking connection between two Virtual Private Clouds (VPCs) that routes traffic using private IPv4 or IPv6 addresses.
  Key Rules:
    No overlapping CIDRs — If both VPCs use 10.0.0.0/16, peering is impossible. This is why CIDR planning matters!
    Not transitive — If A↔B and B↔C, A CANNOT reach C through B. You need a separate A↔C peering.
    Cross-region supported — VPC in Mumbai can peer with VPC in Virginia.
    Cross-account supported — Your VPC can peer with another AWS account's VPC.
    Route tables required — You must manually add routes in BOTH VPCs.

Use peering when:
  Dev VPC needs to access shared services in a Prod VPC
  Two departments need to share a database but keep environments separate
  Cross-account access (your VPC + partner's VPC)
  Don't use peering when you have 10+ VPCs — that's 45 peering connections (n×(n-1)/2). Use Transit Gateway instead."

transit gateway
It is a centralized cloud router that connects multiple VPCs and on-premises networks through a single hub.
What Can Attach to Transit Gateway?
  1. VPC Attachment--> Connects a VPC to the TGW
  2. VPN Attachment-->	Connects a Site-to-Site VPN tunnel
  3. Direct Connect Gateway-->	Connects a dedicated physical line
  4. TGW Peering	Connects--> to a TGW in another region (inter-region)
  5. Connect Attachment-->	For SD-WAN appliances (GRE/BFD)

resources quotas on VPC -> AWS enforces default limits (quotas) on Virtual Private Cloud (VPC) resources. While many of these are "soft limits" that can be raised by submitting a request to AWS Support
  components with default limits->
    CIDR per VPC: 5
    VPCs per Region: 5  
    Internet Gateways per Region: 5 (cannot increase on soft limit)
    NAT gateways per AZ: 5
    Elastic IP's per region: 5
    VPC peering connections per VPC: 50
    Security Group Rules per SG: 60
    Route Tables per vpc: 200
    Network ACLs (NACL) per vpc: 200
    Subnets per VPC: 200
    Routes per route table: 500
    Security Groups per Region: 2500

what are VPC endpoints and types
A VPC Endpoint is a secure, private network connection that allows you to connect your VPC to supported AWS services and VPC endpoint services without using an Internet Gateway, NAT Gateway, VPN connection, or AWS Direct Connect.
  The Two Types of VPC Endpoints
    1. Interface Endpoints - Creates an Elastic Network Interface (ENI) with a private IP address from your subnet to route TCP/UDP traffic securely.
    2. Gateway Endpoints - Acts as a virtual router target in your route table using a prefix list rather than an IP address.

What can be done if we want more NAT gateways before rising a request to AWS
  Add secondary IPs: Associate up to 7 secondary private or Elastic IP addresses to an existing NAT gateway to expand concurrent connection limits per destination.
  Use Gateway Endpoints: Route traffic to Amazon S3 and Amazon DynamoDB via free gateway endpoints instead of a NAT gateway
  Deploy Interface Endpoints: Use AWS PrivateLink interface endpoints for internal service communication to reduce packet load and lower data processing costs
  Consolidate subnets: Share a single well-utilized NAT gateway across multiple smaller private subnets if traffic volume and packet rates remain safely below the 100 Gbps or 10 million packets-per-second thresholds

Osi layers->
Layer 7: Application 
Layer 6: Presentation
Layer 5: Session 
Layer 4: Transport 
Layer 3: Network
Layer 2: Data Link 
Layer 1: Physical 

Elastic Load Balancer (ELB): Automatically distributes incoming application traffic across multiple targets (EC2 instances, containers, Lambdas) in one or more Availability Zones to ensure fault tolerance.

ELB types:
1. Application Load Balancer (ALB) --> Layer 7 --> HTTP/HTTPS
2. Network Load Balancer (NLB) --> Layer 4 --> TCP/UDP/TLS 
3. Gateway Load Balancer (GWLB) --> Layer 3 --> IP
4. Classic Load Balancer (CLB) --> Layer 4/7 --> TCP/HTTP

Auto Scaling Groups (ASG)
It is a service that manages a collection of server instances, automatically adding or removing servers to match fluctuating user traffic. This ensures your application remains responsive during peak times and cost-effective during quiet periods.

Key components
 1. Capacity Settings - Minimum,maximum,desired
 2. Launch Template - The blueprint for new instances. Every instance ASG launches uses this template. lik -> AMI, Instance type, Key pair, Security Groups, User Data, IAM Role, Network, Storage
 3. AZ Distribution - Auto Scaling Groups attempt to distribute instances evenly across the selected Availability Zones to ensure high availability and fault tolerance.
 4 Scaling Policies - There are four primary types of scaling policies:
    *   **Target Tracking Scaling:** Adjusts capacity to maintain a specific metric (e.g., keeping average CPU utilization at 50%).
    *   **Step Scaling:** Adjusts capacity based on the size of the alarm breach (e.g., add 1 instance if CPU is > 60%, add 3 if CPU is > 80%).
    *   **Simple Scaling:** A legacy policy that adjusts capacity based on a single alarm, typically requiring a cooldown period.
    *   **Scheduled Scaling:** Adjusts capacity based on a predictable schedule (e.g., scale up at 8 AM and scale down at 6 PM daily).

Auto scaling Health checks
Auto Scaling health checks monitor your cloud instances and replace any that fail, ensuring your application stays online and healthy.
Main Types of Health Checks
  EC2 Status Checks: Looks at the underlying hardware and hypervisor. If the physical host or instance status fails, Auto Scaling replaces it.
  Elastic Load Balancing (ELB) Checks: Tests if your application responds properly to web requests (HTTP/HTTPS) on a specific path. If the app crashes, the load balancer reports it, and Auto Scaling replaces the instance.
  Custom Health Checks: Lets you push your own health data from an external system directly into Amazon EC2 Auto Scaling.
  Amazon EBS Checks: Monitors storage volume performance and replaces instances if the attached block storage becomes impaired.

SNS (Simple Notification Service)
AWS SNS stands for Amazon Simple Notification Service. It is a fully managed, push-based publish-subscribe (pub/sub) service that lets you send messages or alerts from applications to multiple subscribers or other services instantly.

SNS Core concepts
  Publishers (Producers): Send a message to a logical communication channel called a Topic.
  Topics: A central access point where the message is received and broadcasted.
  Characters-> Name, type (Standard and FIFO), ARN and Max Topics
  Subscribers (Consumers): Register or subscribe to a topic to receive every message sent to it

SNS fanout
An Amazon SNS fanout is a messaging design pattern where a single message published to an Amazon SNS topic is automatically replicated and pushed in parallel to multiple subscribed endpoints, such as Amazon SQS queues, Lambda functions, or HTTP endpoints

SQS (Simple Queue Service)
AWS SQS stands for Amazon Simple Queue Service. It s a fully managed message queuing service that lets you send, store, and receive messages between software components at any volume. 

CloudWatch
CloudWatch is AWS's monitoring and observability service. It does FIVE things:
  Metrics      — Numbers over time (CPU, memory, requests)
  Alarms       — "Alert me when X crosses Y threshold"
  Logs          — Text output from applications and services
  Dashboards    — Visual graphs on a single screen
  Events/EB     — "When X happens, trigger Y" (now called EventBridge)

Common Default Metrics by AWS Service
  Amazon EC2: CPUUtilization, NetworkIn, NetworkOut, DiskReadBytes, and StatusCheckFailed.
  Amazon RDS: CPUUtilization, DatabaseConnections, and FreeStorageSpace.
  Amazon EBS: VolumeReadBytes, VolumeWriteBytes, and VolumeQueueLength.
  Amazon S3: BucketSizeBytes and NumberOfObjects.

Cloudwatch agent
The Amazon CloudWatch Agent is a software package you install on servers to collect system-level metrics, logs, and traces.

Amazon EFS (Elastic File System)
Amazon Elastic File System (EFS) is a serverless, fully elastic file storage service designed to provide shared file systems for compute workloads across multiple Amazon EC2 instances.

EFS Storage Classes:
⚬	EFS Standard (the bustling office — fast, everyday shared access)
⚬	EFS Standard-Infrequent Access (the filing cabinet — cheaper, accessed less often)
⚬	EFS One Zone (the local branch — cheaper, limited to one AZ)
⚬	EFS One Zone-Infrequent Access (the local basement archive — cheapest, rarely accessed)

An Amazon EFS file system supports a hard quota of up to 25,000 concurrent client connections.

Amazon S3 (Simple Storage Service)
Amazon Simple Storage Service (S3) is a highly scalable, object storage service designed to store and retrieve any amount of data from anywhere on the web.

S3 Storage Classes:
⚬	S3 Standard (the display shelf — immediately available, frequently accessed)
⚬	S3 Intelligent-Tiering (the smart organizer — automatically moves items based on changing usage)
⚬	S3 Standard-Infrequent Access (the storage closet — low cost, occasional use)
⚬	S3 Glacier Flexible Retrieval (the offsite vault — very cheap, takes hours to retrieve)
⚬	S3 Glacier Deep Archive (the time capsule — cheapest, takes days to retrieve)

s3 versioning
Versioning in Amazon S3 is a feature that keeps multiple variants or copies of an object in the same bucket.

S3 replication
Amazon S3 Replication is a fully managed, asynchronous service that automatically copies objects and their metadata between S3 buckets.

capex and OpEx -> Capital Expenditure (CapEx) is a major, one-time upfront investment in physical assets.
Operational Expenditure (OpEx) is an ongoing, day-to-day cost spent to run a business without upfront commitments.

What is high Availability -> High availability (HA) in AWS is a system design approach that ensures your applications and data remain accessible with minimal downtime, even if a component like a server or data center fails.

assume role (important) -> It is an AWS STS API operation that grants an application or user temporary security credentials to access AWS resources they normally cannot reach.

What is permission boundary -> A permission boundary is an advanced IAM feature used to set the absolute maximum permissions that an identity-based policy can grant to an IAM entity 

Statefull and stateless ->
In computing and software architecture, stateful and stateless describe whether a system, application, or protocol remembers previous interactions or treats every single request as a completely blank slate.

local and default routes->
local route allows all subnets inside the VPC to talk to each other automatically. A default route, on the other hand, is a rule you manually add to send all unknown, external internet traffic to a gateway.

AWS Key Management Service (KMS)-> It is a fully managed service that makes it easy for you to create and manage cryptographic keys. It controls how data is encrypted across your AWS services and applications.

Service Control Policies (SCPs) and IAM Policies are both JSON structures used to manage access in AWS, but they operate at different levels of the cloud hierarchy.The core difference is that SCPs set the maximum permission boundaries across entire AWS accounts, while IAM Policies grant specific permissions to users and roles within a single account.

measured service
Measured Service is a reference framework defined by the National Institute of Standards and Technology (NIST) stating that cloud systems must automatically monitor, control, optimize, and report resource usage using a built-in metering capability.

what is Elasticity
Elasticity is the ability of a cloud computing system to automatically scale its infrastructure resources up or down in real-time to match changing workload demands

Cloud trail
AWS CloudTrail is a monitoring and logging service that records user activity and API (Application Programming Interface) calls across your Amazon Web Services infrastructure.

Difference between CloudWatch and CloudTrail
  The primary difference is their focus: AWS CloudTrail logs who did what in your account (identity and API actions), while Amazon CloudWatch monitors how your resources are performing (metrics and performance).

How many days of data can CloudTrail keep?
AWS CloudTrail keeps data for 90 days by default in its standard event history, but you can store it much longer using trails or CloudTrail Lake.

If you need CloudTrail data for more than 90 days, what can you do?
Option 1: Send Data to Amazon S3 
Option 2: Use CloudTrail Lake (Best for easy searching) - CloudTrail Lake is a managed feature that lets you store and run database queries on your logs directly inside CloudTrail.

Cloud front
AWS CloudFront (Amazon CloudFront) is a fast, globally distributed content delivery network (CDN) service that securely delivers websites, videos, applications, and APIs to users with low latency and high transfer speeds

How It Works
  Edge Locations: CloudFront delivers content through a worldwide network of data centers called edge locations and points of presence (PoPs).
  Caching Content: When a user requests data, CloudFront routes the request to the nearest edge location to provide the lowest possible delay.
  Origin Servers: If the file is cached at the edge, it is delivered immediately. If not, CloudFront fetches it from an origin server—such as an Amazon S3 bucket, an EC2 instance, or a custom HTTP server.

NFS
In AWS, NFS (Network File System) is not a standalone AWS product, but rather a standard network protocol used by multiple AWS storage services to let Linux-based EC2 instances, containers, and on-premises servers mount and share files over a network. 

ARN
An ARN (Amazon Resource Name) is a unique string that identifies a specific resource in Amazon Web Services (AWS).

AWS Organisations
AWS Organizations is a free account management service that lets you centrally govern and manage multiple Amazon Web Services (AWS) accounts under one unified structure

AWS CLI
The AWS Command Line Interface (AWS CLI) is an open-source, unified tool from Amazon Web Services that allows you to control, manage, and automate AWS cloud resources directly from your local terminal or command prompt

Difference between High availibility, Fault tolerance and disaster recovery
High Availability (HA): Minimizes system downtime through quick, automated switching to backup servers during minor hardware or software failures.
Fault Tolerance: Eliminates downtime completely by using identical, fully mirrored systems that instantly take over without interrupting the user.
Disaster Recovery (DR): Restores the entire system and data from a remote location after a major event like a fire, flood, or widespread outage.

shared responsibility model
The shared responsibility model is a cloud security framework that divides security and compliance duties between your cloud provider and your business

IAM access analizer
AWS IAM Access Analyzer is an Amazon Web Services tool that helps security teams and administrators monitor and analyze access to cloud resources to achieve the principle of least privilege

S3 object lock
Prevents Amazon S3 objects from being deleted or overwritten for a fixed amount of time or indefinitely. It uses a Write-Once-Read-Many (WORM) model and is primarily used to meet regulatory compliance requirements.

client side architecture
Refers to the frontend components (e.g., web browsers, mobile apps, IoT devices) that interact directly with the user. In AWS, this often involves static assets hosted on S3 and distributed via CloudFront, interacting with backend services through API Gateway or AWS Amplify.

server side architecture
Refers to the backend infrastructure handling business logic, data storage, and processing. In AWS, this includes compute services (EC2, ECS, Lambda), databases (RDS, DynamoDB), and internal networking securely deployed within a VPC.

detanative queues
Dead Letter Queues (Assuming "detanative queues")
An Amazon SQS queue or SNS topic destination where messages that fail to process successfully after a maximum number of attempts are sent. This isolates proble

cloudwatch custom matix
Metrics that you manually publish to Amazon CloudWatch from your own applications or scripts (using the PutMetricData API). This allows you to monitor application-specific data (e.g., memory utilization on EC2, user sign-ups) that AWS does not collect by default.

difference between application, network, gateway and classic load balancers
⚬	Application Load Balancer (ALB): Operates at Layer 7 (HTTP/HTTPS). Routes traffic based on URL paths, hostnames, and HTTP headers. Ideal for microservices and modern web applications.
⚬	Network Load Balancer (NLB): Operates at Layer 4 (TCP/UDP). Provides ultra-high performance, ultra-low latency, and uses static IP addresses. Ideal for high-throughput network traffic.
⚬	Gateway Load Balancer (GWLB): Operates at Layer 3/4. Routes traffic to third-party virtual appliances like firewalls and intrusion detection systems.
⚬	Classic Load Balancer (CLB): A legacy load balancer supporting Layer 4 and Layer 7. AWS recommends using ALB or NLB for new deployments.

Purpose of default route
In a VPC Route Table, the default route (typically 0.0.0.0/0 for IPv4) acts as a catch-all. It directs any network traffic that does not explicitly match other specific routes to a designated gateway, such as an Internet Gateway (for public access) or a NAT Gateway (for outbound-only internet access).

DNS resolution
The process of translating human-readable domain names (e.g., example.com) into IP addresses. Amazon Route 53 provides highly available public and private DNS services. Within a VPC, the default AWS DNS resolver (AmazonProvidedDNS) automatically resolves internal hostnames.

steps for vpc peering connections
	1.	Request: The owner of the requester VPC initiates a peering request to the accepter VPC (can be in the same or a different AWS account/Region).
	2.	Accept: The owner of the accepter VPC accepts the peering request.
	3.	Update Route Tables: Both VPCs must update their respective route tables to point traffic destined for the peer's CIDR block to the peering connection ID.
	4.	Update Security: Adjust Security Groups and Network ACLs in both VPCs to allow the desired inbound and outbound traffic.

What is meant by transitive
In AWS networking, transitive routing means routing traffic through an intermediate network to reach a final destination. 

Can IAM role have passwords --> No

Is S3 Regional
Yes. While the S3 namespace is global (bucket names must be unique across all AWS accounts) and the console provides a global view, the actual data is stored in a specific AWS Region chosen at the time of bucket creation. Data does not leave that region unless explicitly configured to do so (e.g., Cross-Region Replication).

Data encryption and types
⚬	Server-Side Encryption (SSE): AWS handles encryption before saving to disk. Options include SSE-S3 (Amazon managed keys), SSE-KMS (KMS managed keys), and SSE-C (Customer provided keys).
⚬	Client-Side Encryption (CSE): You encrypt the data within your application before sending it to AWS.

Decoupling
An architectural pattern where distributed system components remain independent and unaware of each other's underlying operations. If one component fails, it does not bring down the entire system. In AWS, this is achieved using asynchronous integration services like Amazon SQS (message queues), Amazon SNS (pub/sub notifications), and Amazon EventBridge.

VPC flow logs
A feature that captures metadata about the IP traffic flowing into, out of, and across network interfaces in your VPC. The logs are published to Amazon CloudWatch Logs or Amazon S3 and are essential for troubleshooting network connectivity, auditing security group rules, and monitoring network traffic patterns.

Multitire architecture
A software architecture that divides an application into logical, distinct tiers—typically a Presentation (Web) tier, an Application (Logic) tier, and a Data (Database) tier. In AWS, this is physically mapped by deploying resources into different subnets (e.g., public subnets for web servers, private subnets for application servers and databases) to restrict access and improve security.

AMS
A service where AWS operates your cloud infrastructure on your behalf. AMS provides an enterprise-grade operating model, handles day-to-day management (patching, backups, monitoring, incident response), and ensures compliance and security, allowing organizations to focus on application development rather than infrastructure operations.

amazon inspector
an automated vulnerability management service that continually scans your cloud resources for security flaws and network exposure.

How does Inspector get into/access the EC2 instance? Does it require any access?
Amazon Inspector accesses EC2 instances using either an agent-based method via the AWS Systems Manager (SSM) Agent or an agentless method using Amazon EBS volume snapshots, depending on your configured scan mode.

If SSM is not configured and Inspector uses an EBS snapshot, is the snapshot alone enough to identify vulnerabilities?
Yes, the EBS snapshot alone is enough for Amazon Inspector to identify software vulnerabilities.

If Inspector finds a vulnerability, what is the next action?
1. Review and Prioritize (Find the Worst Bugs First) - Inspector gives every vulnerability an Amazon Inspector Risk Score. This is a smart score that looks at how dangerous the bug is and whether your instance is actually exposed to the internet.
2. Remediate (Fix the Problem) - The finding will tell you exactly what is wrong and how to fix it.
3. Automate the Next Actions (Optional) - Most companies do not just look at the Inspector dashboard. They set up AWS to automatically route the alert to the right people:
4. Re-scan and Close - Once you update the software or change the network settings, Inspector will notice the change during its next automatic scan. If the vulnerability is gone, Inspector will automatically change the status of the finding to Closed.

Where will the Inspector finding be sent?
By default, Amazon Inspector findings are automatically sent to the Amazon Inspector Console.

How will the finding be triggered?
An Amazon Inspector finding is not triggered by a person clicking a button. Instead, Inspector is fully automated and runs continuously in the background.

How will all the team members be informed about the vulnerability?
To inform all your team members about a vulnerability, you need to connect Amazon Inspector to the communication tools your team already uses every day.

Does Inspector scan EC2 instances and container images? What other capabilities does it have?
It scans Amazon EC2 instances for operating system and application bugs. For container images, it integrates directly with Amazon ECR (Elastic Container Registry) to inspect your container layers. and also scans AMI's

Is the scanning Amazon Inspector in scheduled or triggered?
Amazon Inspector relies primarily on an event-driven, triggered approach rather than a traditional clock-based schedule.

Lambda has a 15-minute maximum execution time — how does Inspector scan Lambda?
Amazon Inspector does not execute your Lambda functions to scan them; instead, it performs static analysis by directly inspecting the function's deployment package and code stored in AWS Lambda.

If the Lambda function takes more than 15 minutes, what will you do?
AWS Lambda has a hard maximum timeout limit of 15 minutes (900 seconds), so the function will be automatically terminated by the service when it reaches this limit.

what is Amazon Macie
Amazon Macie is a fully managed data security and privacy service that uses machine learning and pattern matching to discover, classify, and protect sensitive data stored in Amazon S3 and also scans ECR images

Is Amazon Macie scanning scheduled or triggered?
Amazon Macie uses a combination of both scheduled and continuous automated processes.

If an object is put into an S3 bucket, does Macie scan immediately, at a particular time, or at the end of the day?
When you put a new object into an Amazon S3 bucket, Amazon Macie does not scan it immediately out-of-the-box.
1. Automated Sensitive Data Discovery (Daily Evaluation) - If you are using Macie's default Automated Sensitive Data Discovery feature, Macie runs a daily evaluation cycle
2. Targeted Discovery Jobs (Scheduled by You) - If you created a custom Sensitive Data Discovery Job, you control the timing
3. Immediate Scanning (Requires Custom Setup) - If you absolutely need the object to be scanned immediately upon upload, Macie cannot do this natively because its backend architecture is optimized for processing large batches of data rather than real-time streams.

Where are Macie findings sent?
the raw details are saved inside the Macie Console. 

Are the findings sent to CloudWatch or somewhere else?
No, raw security findings are not sent directly to CloudWatch Logs. Instead, Amazon Macie publishes its findings to Amazon EventBridge (which used to be called CloudWatch Events)

How does image scanning work after the image is pushed to ECR?
the scanning workflow activates automatically based on the specific scanning frequency you have configured
Step 1: The Initial Trigger (Scan on Push)
Step 2: Running the Scan (Basic vs. Enhanced)
Step 3: Life After the Push (The Re-Scan Mechanism)
Step 4: Outputting Results

AWS WAF
AWS WAF (Web Application Firewall) is a cloud security service that protects web applications and APIs from common web attacks and malicious traffic

What kind of rules do we add in AWS WAF?
AWS WAF rules tell the firewall exactly what traffic to let in and what traffic to block. You can mix and match different types of rules to protect your application.
1. Managed Rule Groups - These are pre-made rules created and updated by AWS or security experts.
2. Custom Rules (Rules You Make) - You can create your own rules to match specific patterns in the incoming traffic.
3. Advanced Rules - These rules look at the behavior of the traffic rather than just the code.

Where can WAF be configured? Which AWS services can use WAF?
AWS WAF cannot be deployed anywhere as a standalone tool. Instead, it must be attached directly to specific AWS services that handle your incoming web traffic like Amazon CloudFront, Application Load Balancer (ALB), Amazon API Gateway, AWS AppSync, Amazon Cognito.

If the image is in ECR, how does Trivy get the image to scan?
Trivy connects directly to Amazon Elastic Container Registry (ECR) using your built-in AWS credentials and pulls the image layers locally for analysis.

Is GuardDuty enabled by default?
Amazon GuardDuty is a managed threat detection service that continuously monitors your AWS accounts, workloads, and data for malicious or unauthorized behavior

Is GuardDuty a global service or region-specific service?
region specific

Cross region replication

Can we attach one EBS from one region to another region
No

What is meant by scaleup/scaledown and scalein/scaleout

types of IAM policies (restructered)

Types of monitoring in AWS and while explaing give examples of few matrices of different services

detach keys and modifications

three states of alaram

explain a proper features of s3 storage classess and also lifecycle policies

How to transfer the resources from one account to another

What are the multiple aspects we will look before deploying a service in a specific region

Session persistance or stickyness in load balancer

How S3 helps in static website hosting